# NodeServerWithCSVChart

Install nodejs.
cd to folder with server.js
start server with node server.js
Open http://localhost:8080/ to see bar chart.

Server reads csv in CSVs directory and displays its contents as bar chart.